{
  "author": "@wanner.work",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "format": "prettier --write ./packages/**/*.{tsx,ts,json}",
    "build": "pnpm --filter='./packages/**/*' --parallel run build"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-organize-imports": "^2.3.4"
  }
}