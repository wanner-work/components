{
  "author": "@wanner.work",
  "license": "MIT",
  "scripts": {
    "format": "pnpm --filter='./packages/**/*' --parallel run format",
    "build": "pnpm --filter='./packages/**/*' --parallel run build"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-organize-imports": "^2.3.4"
  }
}
